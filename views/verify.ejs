<!DOCTYPE html>
<html lang="en">
    <%- include('partials/header.ejs', {title: 'Verify'}) %>
    <body>
        <div
            class="container-fluid d-flex justify-content-center align-items-center pt-3 flex-column"
        >
            <% if (messages.error) { %>
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <%= messages.error %>
            </div>
            <% } %> <% if (messages.success) { %>
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <%= messages.success %>
            </div>
            <% } %>
            <h3>An verification email has sent to your email <%= email %></h3>
            <form action="/verify/resend" method="get" class="mt-3">
                <div class="form-group">
                    <button name="submitBtn" type="submit" class="btn btn-outline-primary">
                        Resend Email
                    </button>
                </div>
            </form>
            <form action="/auth/delete" method="get" class="mt-3">
                <div class="form-group">
                    <button name="submitBtn" type="submit" class="btn btn-danger">
                        Delete My Account
                    </button>
                </div>
            </form>
        </div>
    </body>
</html>
